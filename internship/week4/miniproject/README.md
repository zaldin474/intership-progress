# Week 4 — Mini-Project

A duplicate copy of the [Day 5](../day5/) leak-free pipeline notebook, kept in its own `miniproject/` folder as the week's checkpoint deliverable.

## Contents
- **`jupy_day5.ipynb`** and **`diabetes_prediction_dataset.csv`** — same cleaning, EDA, engineered features, `ColumnTransformer` + `Pipeline` setup, baseline cross-validation, and `GridSearchCV` tuning as Day 5.

See the [Day 5 README](../day5/) for the full breakdown of what the notebook does.
